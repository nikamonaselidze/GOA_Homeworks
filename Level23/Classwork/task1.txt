#1)კომენტარის მეშვეობით ახსენით .lower() / .upper() / .capitalize() / .find() ფუნქციების დანიშნულება
.lower() გამოიყენება ცვლადის ინფორმაციის პატარა ასოებით დაწერაში.
.upper() გამოიყენება ცვლადის ინფორმაციის დიდი ასოებით დაწერაში.